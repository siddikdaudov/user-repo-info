import { API_URL } from "./api-url";

export const ENDPOINT_GET_REPOSITORIES = `${API_URL}/search/repositories`;
export const ENDPOINT_GET_REPOSITORY = `${API_URL}/repos`; // + юзернейм, + репозиторий
